# =============================================================================
# FYP Unified Project — Requirements
# All processing is local; no cloud dependencies.
# =============================================================================

# -- Web Framework (FastAPI backend) ------------------------------------------
fastapi
uvicorn
python-multipart

# -- Data Processing -----------------------------------------------------------
pandas
openpyxl
numpy
scipy
networkx
cugraph

# -- GPU Acceleration (optional — requires CUDA-capable NVIDIA GPU) -----------
# Install the variant matching your CUDA version, e.g.:
#   pip install cupy-cuda11x   (CUDA 11.x)
#   pip install cupy-cuda12x   (CUDA 12.x)
# cupy-cuda11x
pycuda
cupy-cuda13x
# pycuda

# -- Visualization ------------------------------------------------------------
matplotlib
seaborn

# -- Frontend Build -----------------------------------------------------------
# The React/Vite frontend lives in webapp/frontend/.
# Run: npm install  (once)
#      npm run dev  (development server)
#      npm run build  (production build)
# Node.js >= 18 required.
